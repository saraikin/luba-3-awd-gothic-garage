# Legacy asset directory

Binary texture files are no longer required. The current stone masonry and aged-wood textures are generated deterministically by `../scripts/textures.py`, making the project self-contained and directly editable.
